{
  "bsonType": "object",
  "required": ["username", "password"],
  "properties": {
    "_id": {
      "description": "用户唯一标识符",
      "bsonType": "string"
    },
    "username": {
      "description": "用户名",
      "bsonType": "string"
    },
    "password": {
      "description": "密码（加密存储）",
      "bsonType": "string"
    },
    "vehicles": {
      "description": "绑定的车辆信息",
      "bsonType": "array",
      "items": {
        "bsonType": "object",
        "properties": {
          "vehicleId": {
            "description": "车辆ID",
            "bsonType": "string"
          },
          "licensePlate": {
            "description": "车牌号",
            "bsonType": "string"
          }
        }
      }
    }
  }
}
